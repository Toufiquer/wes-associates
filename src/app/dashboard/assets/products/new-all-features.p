Look at the file 'src/app/dashboard/assets/products/page.tsx' inside the file you found a model For adding products. there is found a field named 'Description' this field is Rich Text Editor. Now Update it so I can clearly See code view and Preview with tags and make sure it working for Heading, Bold, Italic, line throw, alignment, list item, and heigh light. And you have to make sure it will work on View Model, and Edit Model also. 

Look at the file 'src/app/dashboard/assets/products/page.tsx' Inside the file you found a model Named 'Add Product' inside model you found a field named 'RichTextEditorField' and here is the file path for RichTextEditorField 'src/components/dashboard-ui/RichTextEditorField.tsx' Now Your task is Update those part as the follwoing instructions.

1. Add product Model.
    - For the Description field there is only Two Tabs named 'Code', 'Preview' and make sure If Select H1 then the font-size will change in preview. Now in preview all heading size are same. fix  it.

2. View Model. 
    - Inside View Model please render only Preview tabs. there is not need to show Code tabs.

3. Edit product Model.
    - Do as the Add product model but with edit options.

4. Make sure RichTextEditorField is render properly as Preview inside Query.tsx 'src/components/all-pages/page-5/Query.tsx' 


Look at the Query.tsx 'src/components/all-pages/page-5/Query.tsx' and update it as the following instructions. 
1. add Loading for Image.
2. First there is two div in Desktop in left side there is Image. and in right side there is Button and Others.
3. After those two div below there is description. It will take full width. 





Look at the file 'src/app/dashboard/assets/products/page.tsx' inside the file you found model For adding products, Update Products, and View Products. Now do the follwoing task.

I want to implement a new field called Upload Product. It will save zip file in uploadthings. here is example how to implement 'src/app/dashboard/media/example/uploadthings' and make sure you have to use small button to open the pop-up and after success fully upload please show a success tost.