Look at the page 'src/app/dashboard/my-products/page.tsx' Update the page.tsx as the following instructions.
1. First fetch all products from 'src/app/dashboard/assets/products/page.tsx'
2. than fetch my-orders from 'src/app/dashboard/assets/orders/page.tsx'
3. Then If Products is Parchase and payment is done then render the products with download button. if not then render the products with buy now Button. and implement Cart option with buy now button. 
