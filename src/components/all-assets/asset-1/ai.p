Look at the file named membership 'src/app/dashboard/assets/membership/page.tsx' Update the file  as the following instructions.
1. Add a button named 'Add Combo' 
    - it  will open model and there are fields like 
        * title
        * description
        * Real Price
        * discount Price
        * End date 
2. Update a button named 'Add member'
    - it will open a model and insdie the model there is a search bar.
        * from the search bar placeholder='01711' and after 5 char it will fetch data from account and it will add to db.
        * from user table I can see user info, membership info. and also implement update and delete features. for update please  render a pop-up and there display all membership plan. I can check each membership.

So in the page You have to manage two type of Data. 
    - Member Ship plan
    - Users [got the membership]

Now Look at the membership page 'src/app/membership/page.tsx' and update the file as the following instructions. 
1. It will render all membership plan. If user got membership then render a accordian button named 'Download' and if not get membership plan then render Get membership. it will open a modle like order and after successfull payment it will create account to update it as like 'Buy Now' 
