Look at the folder 'src/app/components/all-assets' and Update all file with the following instructions.
1. Asset-1
    - 'src/app/components/all-assets/asset-4/data.ts'
    - 'src/app/components/all-assets/asset-4/Mutation.tsx'
    - 'src/app/components/all-assets/asset-4/Query.tsx'

Now implement those features as the following instructions.
1. Update the Mutation.tsx as my instructions.
    - At the top there is a field name title. and it will Editable default name is "All Theme"
    - There is a button name Import Product. it will open a popup and inside the pop-up there will display all products form 'src/app/dashboard/assets/products/page.tsx' and It will only display Product Name and Product UID(if Found). at the right there is a check box. at the bottom there is a button named import. It will import products as selected. 
    - There is a Button for Sort. and three sort-able is 
        * Assending
        * Dessending
        * Custom
    - If short custom is selected then Add a features to drug and drop.
    - Add a button name 'update  See More" It will  update button configaration. for 'see more' I want to edit name, url, 

2. Update the Query.tsx as my instructions
    - It will render the data based on Mutation.tsx and it will render card using 'RenderItim.tsx'    


Look at the color combination, border, text, and style form 'src/app/dashboard/assets/page.tsx' and implement those style inside 'src/components/all-assets/asset-4/Mutation.tsx'

Please remove text. and use have to use div and boder to visualise the 'VIEW GRID' AND 'MOBILE VIEW'

If I click 'VIEW GRID' than the Imported Products will show insdie Mutation.tsx as selected.








Look at the folder 'src/app/components/all-assets' and Update all file with the following instructions.
1. Asset-5
    - 'src/app/components/all-assets/asset-5/data.ts'
    - 'src/app/components/all-assets/asset-5/Mutation.tsx'
    - 'src/app/components/all-assets/asset-5/Query.tsx'
    - 'src/app/components/all-assets/asset-5/RenderItem.tsx'

Some Item is render currectly but some item render with narrow width. Please fix it. and make sure all item render with same width and same height. and also make sure responsive is working properly.